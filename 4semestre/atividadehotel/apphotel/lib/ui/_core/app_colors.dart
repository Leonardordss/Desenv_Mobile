// Arquivo para estilizar as cores do aplicativo

import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

abstract class AppColors {
  static const Color backgroundColor = Color(0xFFF2E7DC);       // Cor de fundo clara
  static const Color mainColor = Color(0xFF6D9886);             // Cor principal (verde-oliva suave)
  static const Color lightBackgroundColor = Color(0xFFC4C1A4);  // Fundo alternativo
  static const Color fundoCards = Color(0xFFE5DCC3);            // Fundo de cards
  static const Color darkText = Color(0xFF393E46);              // Texto escuro e botões
}
